import { Wallet } from '@terra-money/terra.js';
import { ContractConfig, ContractRef, InstantiateMessage } from '../config';
import { LCDClientExtra } from './LCDClientExtra';
export declare type DeployHelpers = {
    build: (contract: string) => Promise<void>;
    optimize: (contract: string) => Promise<void>;
    storeCode: (contract: string, signer: Wallet, options?: {
        noRebuild?: boolean;
    }) => Promise<number>;
    instantiate: (contract: string, signer: Wallet, options?: {
        codeId?: number;
        instanceId?: string;
        admin?: string;
        init?: InstantiateMessage;
    }) => Promise<string>;
};
export declare type Env = {
    config: (contract: string) => ContractConfig;
    refs: {
        [contractName: string]: ContractRef;
    };
    wallets: {
        [key: string]: Wallet;
    };
    defaultWallet: Wallet;
    client: LCDClientExtra;
    deploy: DeployHelpers;
};
export declare const getEnv: (configPath: string, keysPath: string, refsPath: string, network: string, defaultWallet: string) => Env;
