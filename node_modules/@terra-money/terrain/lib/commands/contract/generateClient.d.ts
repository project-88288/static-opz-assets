import { Command, flags } from '@oclif/command';
export default class GenerateClient extends Command {
    static description: string;
    static flags: {
        'lib-path': flags.IOptionFlag<string>;
        dest: flags.IOptionFlag<string>;
        'build-schema': import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    static args: {
        name: string;
        required: boolean;
    }[];
    run(): Promise<void>;
}
