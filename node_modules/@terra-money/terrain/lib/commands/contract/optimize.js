"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const deployment_1 = require("../../lib/deployment");
const flag_1 = require("../../lib/flag");
const config_1 = require("../../config");
class Optimize extends command_1.Command {
    async run() {
        const { args, flags } = this.parse(Optimize);
        const globalConfig = config_1.loadGlobalConfig(flags['config-path']);
        await deployment_1.optimize({
            contract: args.contract,
            useCargoWorkspace: globalConfig.useCargoWorkspace,
        });
    }
}
exports.default = Optimize;
Optimize.description = 'Optimize wasm bytecode.';
Optimize.flags = {
    'config-path': flag_1.configPath,
};
Optimize.args = [{ name: 'contract', required: true }];
